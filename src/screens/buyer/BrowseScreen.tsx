// Replaced by HomeScreen, which shows the same listings as horizontal rails
// (추천 / 가까운 거리에 / 특별 할인) instead of one flat list. Kept only because
// files here can't be removed outright — nothing imports it.
export {};
